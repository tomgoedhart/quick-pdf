# AWS S3 Configuration
NODE_AWS_REGION=your-aws-region
NODE_AWS_ACCESS_KEY_ID=your-aws-access-key
NODE_AWS_SECRET_ACCESS_KEY=your-aws-secret-key
NODE_API_S3_KEY=your-api-s3-key

# Synology Configuration
# Set to true to enable Synology storage instead of S3
USE_SYNOLOGY=true
# The base URL for the Synology API
SYNOLOGY_BASE_URL=https://quickgraveer.synology.me:5001/webapi/entry.cgi
# The base path in the Synology storage
SYNOLOGY_BASE_PATH=/data/quick-opslag
# The Synology session ID - this is required for authentication
# To get a SID, log in to the Synology DSM web interface and use the browser's
# developer tools to find the 'id' cookie, or use the Synology API auth endpoint:
# curl -X GET 'https://quickgraveer.synology.me:5001/webapi/auth.cgi?api=SYNO.API.Auth&method=login&version=3&account=YOUR_USERNAME&passwd=YOUR_PASSWORD&session=FileStation&format=cookie'
SYNOLOGY_SID=your-synology-sid

# Other Configuration
NODE_ENV=production
# Set to true to disable S3 uploads
# NODE_DISABLE_S3=true

# SendGrid Configuration
SENDGRID_API_KEY=your-sendgrid-api-key
